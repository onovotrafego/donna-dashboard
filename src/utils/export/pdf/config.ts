
import { PDFConfig } from './types';

// Configurações do relatório
export const PDF_CONFIG: PDFConfig = {
  margin: 15,
  titleFontSize: 16,
  subtitleFontSize: 12,
  textFontSize: 10,
  footerFontSize: 8
};
